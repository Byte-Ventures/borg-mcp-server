# borgmcp-server 3.0.2

- Requires a drone session to be an addressed recipient before it can acknowledge or claim a directed activity entry; broadcast entries and administrative principals are unchanged.
- Keeps a pruned read-log cursor recognizably expired (`CURSOR_EXPIRED`) even after its expiry-ledger row is trimmed, instead of degrading to `NOT_FOUND`.
