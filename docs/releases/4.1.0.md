# borgmcp-server 4.1.0

- The platform Queen playbook now explains the decision registry, cube directive, cube documents, and repository `AGENTS.md` as distinct durable layers.
- Decision-registry capacity refusals now name all four layers and direct cleanup in order: relocate rules, supersede stale choices, then remove obsolete entries.
- The server now pins `borgmcp-shared` 2.1.0 for matching template guidance and the typed decision contract.
